# Development/test-only tooling (HARDENING_PLAN.md finding L7).
#
# Not installed into the runtime image — backend/Dockerfile only installs
# requirements.txt. Install both for local development/CI:
#   pip install -r backend/requirements.txt -r backend/requirements-dev.txt
-r requirements.txt
pytest==8.2.2
pytest-asyncio==0.23.7
black==24.4.2
isort==5.13.2
